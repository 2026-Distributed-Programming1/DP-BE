spring:
  application:
    name: DP-BE
  datasource:
    url: jdbc:mysql://localhost:3306/insurance_db?useSSL=false&serverTimezone=Asia/Seoul&allowPublicKeyRetrieval=true&characterEncoding=UTF-8
    username: admin
    password: 1234
    driver-class-name: com.mysql.cj.jdbc.Driver

# 기동 시 초기 데이터 시더 (DataSeeder). 실 DB 보호용으로 false 가능.
app:
  seed:
    enabled: true